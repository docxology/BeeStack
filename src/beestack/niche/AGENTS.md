# niche/ - BeeNiche

- Keep comb geometry in `comb.py`, heat flow in `thermal.py`, and landscape
  scoring in `landscape.py`.
- Do not mix file I/O or plotting into Niche logic.
- Maintain explicit brood temperature targets and comb voxel constraints from
  config.
