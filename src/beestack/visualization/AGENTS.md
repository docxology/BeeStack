# visualization/ - BeeStack

- Keep figure builders deterministic and data-driven.
- Do not run simulations here; consume records created by orchestrators.
- Return artifact paths so scripts can report outputs.
